Protocol:
  # Headers are a common piece of text that all commands share(at beginning of message)
  Header:
    '$'
  # Headers are a common piece of text that all commands share(at end of message)
  Footer:
    '>'
  Delimiter:
    ' '
  Pattern:
    1: Header
    2: Command
    3: Delimiter
    4: Value
    5: Footer
  Commands:
    GetAnalogZero: "A0"
